/* We shall put all http utilities here.
   Including parsing request, makeing up http response */

/* respones a made up html error message to client */
void resp_err(int errcode);

/* TODO:
   int parse_req(struct pre_req);
   ...
 */
